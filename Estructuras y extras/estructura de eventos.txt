const { Client, Message } = require('discord.js')

module.exports = {
    name: "",
    /**
     * @param {Client} client
     * @param {Message} message
     */
    async execute(client, message) {

    }
}